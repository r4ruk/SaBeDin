client_service:
 - handle different params

request_handler:
 - develop a method, which handles errors and errorstatus codes and gives back
   a Result which can then be returned generally
 - currently statically implemented on line 35

  - DEBUG Handler if query params are extracted as expected:
  send curl:
  curl '127.0.0.1:7878/client?param1=1&param2=asdf'


  src/core/client.rs
// TODO think about exception handling


  - 1. Introduce BL layer

  - 2. Add persistence layer

  - 3. Add seed data minimum for users. (for authentication)

  - 4. Add authentication Middleware which uses
